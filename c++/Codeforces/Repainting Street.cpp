#include<cstdio>
using namespace std;
int n, k;
int c[100010];

int main(){
    int t; scanf("%d", &t);
    while(t--){
        scanf("%d %d", &n, &k);
        for(int i = 1; i <= n; i++) scanf("%d", &c[i]);
        
    }
}