题意：
文件：
    (1)、名称——字符串
    (2)、最大内存为2 ^ 63,long long
    (3)、两个属性: 目录属性、隐藏属性
在当前目录上进行下列活动：(默认目录为根目录)
(1)、文件与寻址：
    a、全路径地址,用/间隔,例如 /home/acm/uva
    b、相对路径地址，如acm的两个子目录vua和uva,现在在vua,如果想表示uva,可以为
    ../uva,..表示当前目录的父目录或在其出现之前在地址字符串列出的目录的父目录,
    ../../../home/..表示根目录,../..表示/home, .表示当前目录。如果当前目录为
    home/acm,./vua等价于/home/acm/vua。我们可以省略./,vua等价于./vua

8个基本命令：
1、cd path：
    cd将当前目录更改为path。path可以使用全路径地址或相对路径地址
    如果不存在输出path not found
2、touch filename[-size][-h]：
    touch命令用于创建或修改文件,filename为路径,其最后一个元素为要创建或修改的文
    件名(一定有效)。路径中的所有元素,有应该是现有目录的有限地址。
    如果值顶的元素不存在,那么新建一个文件。
    如果存在于目录中,那么先将其删除,再新建一个。
    [-size]为文件大小,若没有则为0,[-h]表示文件的属性为隐藏。
    如果指定的目录无效,那么输出path not found
    如果师徒创建一个于当下目录中的一个目录同名的文件,输出
    a directory with the same name exacmts
3、mkdir path [-h]：
    mkdir命令用于创建目录,path为一个地址,最后一个元素为要创建的目录名,其他的部分
    有效,定位一个现有的目录,如果地址无效,输出path not found
    如果已经存在, 输出a directory with the same name exacmts
    如果有[-h]那么表示隐藏
4、find filename [-r] [-h]：
    查找文件,filename为有效地址,如果无效输出path not found
    如果有[-r]表示需要搜索该文件夹下的所有文件,一级其子文件夹里的所有文件,如果没有
    只需要搜索最上面的一层即可。
5、pwd：
    以全路径地址输出目前位置。
6、exit：
    删库跑路并重启系统。
7、grep "string"：
    grep是具有特殊用法形式的特殊命令。接受一个字符串并在其标准输入中搜索改字符串的
    所有匹配项,并输出标准输入中包含这些匹配项的所有行。在bush中grep标准输入只能通
    过一种名为管道的技术提供给它。管道已command1|command2的形式使用。执行指令将会
    导致bash首先执行command1然后将其输送产地给command2,因此要想grep提供一些输入,
    使用管道例如ls | grep “acm”在 ls指令的结果中搜索包含单词acm的所有行。
8、ls [-h] [-r] 或 ls [-s] [-S] 或 ls[-f] [-d](傻逼洛谷不给翻译cnm)
    ls是一个非常流行的命令，用于列出文件系统的[部分]内容。最简单的形式，ls不接受任
    何参数。在本例中，当前没有hidden属性的目录以与find类似的格式列出命令。如果当前
    目录中没有这样的文件或目录，它将打印消息“[空]”。如果path被指定为参数并且是现有
    目录的地址，那么ls将执行在路径而不是当前目录上操作。
    在其他参数中，如果指定了-h，则具有hidden属性的文件和目录也显示了。在指定-r的情
    况下，ls将在中的目录上执行其操作要递归搜索的原始目录。如果指定了-s，则首先对结
    果进行排序不按大小降序排列。在这里，如果有两个文件大小相同文件的顺序应该类似于
    find命令。作为参数，-S的功能类似于-S，而不是-S对文件进行排序大小按非递增顺序排
    列。参数-d强制ls只显示结果中的目录，而-f强制ls只显示不具有directory属性的文件。

    如果某一指令没有按照规定输入，输出bad usage
    如果一个命令多传输了一些参数，或者不属于上面的命令，则输出no such command